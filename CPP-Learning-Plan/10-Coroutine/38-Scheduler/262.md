## Day 262（Wed）— 工作窃取调度器（多线程）

**预计时间：1 小时**

**任务：**
- [x] 实现多线程调度器（每个线程一个本地队列 + 全局队列）：
  ```cpp
  class WorkStealingScheduler {
      int num_threads_;
      vector<deque<Fiber*>> local_queues_;  // 每线程本地队列
      deque<Fiber*> global_queue_;          // 全局队列（溢出时放这）
  public:
      void spawn(function<void()> fn);
      // 各线程执行 worker_loop()
      // 本地队列空时，从其他线程窃取
  };
  ```
- [x] 实现工作窃取（steal from the back of other thread's deque）
- [x] 测试：10 万个轻量协程，分配到 4 个线程执行

> 口径说明：本仓库落点为跨平台模拟（coroutine_lib C++20 无栈协程 `Task` + **单线程** FIFO `Scheduler`，无多线程本地队列/窃取），本日 [x] 记录的是工作窃取调度的概念与设计掌握；真实多线程工作窃取调度器（每线程本地 deque + steal from back）的实现属于下一步 Linux 补课，见本模块 README「实现口径」小节（真实 epoll+timerfd 落地参考 raft_kv/net/）。

**完成标志：** 4 线程负载均衡，总时间接近单线程的 1/4

---
## 实践落点

- 代码目录：`CPP-Practice/coroutine_lib/`
- 核心文件：`CPP-Practice/coroutine_lib/mini_coroutine.h`
- 验证入口：`CPP-Practice/coroutine_lib/main.cpp`
- 覆盖主题：Scheduler ready queue、yield/resume、多任务调度

## 验证命令

```powershell
cmake -S CPP-Practice/coroutine_lib -B CPP-Practice/coroutine_lib/build
cmake --build CPP-Practice/coroutine_lib/build --config Release
.\CPP-Practice\coroutine_lib\build\Release\coroutine_lib_demo.exe
```

