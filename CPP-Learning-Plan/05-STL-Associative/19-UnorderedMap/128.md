## Day 128（Tue）— rehash + 负载因子

**预计时间：1 小时**

**任务：**
- [x] 实现 `load_factor()` = `size_ / buckets_.size()`
- [x] 实现 `rehash(new_bucket_count)`：
  ```cpp
  void rehash(size_t new_n) {
      // 新桶数组 ≥ new_n，且是质数（选质数减少碰撞）
      std::vector<Node*> new_buckets(next_prime(new_n), nullptr);
      // 将所有节点重新挂到新桶
      for (auto* head : buckets_)
          while (head) { ... }  // 逐节点迁移
      buckets_ = std::move(new_buckets);
  }
  ```
- [x] 在 `insert` 中自动触发 rehash（当 load_factor > max_load_factor_）
- [x] 实现质数序列（28 个质数：53, 97, 193, ... 用于桶数选取）
- [x] 测试：插入 1000 元素，观察 rehash 触发次数

**完成标志：** load_factor 始终不超过 0.75

---
## 实践落点

- 代码目录：`CPP-Practice/stl_associative/`
- 核心文件：`CPP-Practice/stl_associative/mini_associative.h`
- 验证入口：`CPP-Practice/stl_associative/main.cpp`
- 覆盖主题：MyUnorderedMap 开链哈希、负载因子、rehash、erase

## 验证命令

```powershell
cmake -S CPP-Practice/stl_associative -B CPP-Practice/stl_associative/build
cmake --build CPP-Practice/stl_associative/build --config Release
.\CPP-Practice\stl_associative\build\Release\stl_associative_demo.exe
```

