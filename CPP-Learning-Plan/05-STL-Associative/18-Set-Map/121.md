## Day 121（Tue）— MyMap 封装

**预计时间：1 小时**

**任务：**
- [x] 创建 `mystl/map.h`，key 用于排序，value 可修改：
  ```cpp
  template<typename Key, typename Value,
           typename Compare = std::less<Key>,
           typename Alloc = std::allocator<std::pair<const Key, Value>>>
  class map {
      using tree_type = RBTree<Key, pair<const Key, Value>,
                                select1st<pair<const Key,Value>>, Compare, Alloc>;
      tree_type tree_;
  public:
      Value& operator[](const Key& k) {
          auto [it, ok] = insert({k, Value{}});
          return it->second;
      }
  };
  ```
- [x] 实现 `operator[]`（不存在则插入默认值）
- [x] 实现 `at(key)`（不存在则抛 std::out_of_range）
- [x] 测试：词频统计（`map<string,int>` 统计文本词频）

**完成标志：** `map[key]++` 词频统计结果正确

---
## 实践落点

- 代码目录：`CPP-Practice/stl_associative/`
- 核心文件：`CPP-Practice/stl_associative/mini_associative.h`
- 验证入口：`CPP-Practice/stl_associative/main.cpp`
- 覆盖主题：MySet/MyMap/MyMultiSet/MyMultiMap 与 lower/upper/equal_range

## 验证命令

```powershell
cmake -S CPP-Practice/stl_associative -B CPP-Practice/stl_associative/build
cmake --build CPP-Practice/stl_associative/build --config Release
.\CPP-Practice\stl_associative\build\Release\stl_associative_demo.exe
```

