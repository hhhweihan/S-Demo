## Day 226（Tue）— 时间轮设计

**预计时间：1 小时**

**任务：**
- [x] 理解时间轮原理：
  ```
  槽数组（如 3600 个槽 = 1小时，每槽 1 秒）
  指针每秒移动一格，执行当前槽的所有定时器
  超过一圈的定时器：存到轮次字段，每圈减 1，轮次为 0 时才执行
  ```
- [x] 实现 `TimingWheel`：
  ```cpp
  class TimingWheel {
      int slots_;            // 槽数量（如 3600）
      int tick_ms_;          // 每格毫秒数（如 1000ms）
      int current_slot_ = 0;
      vector<list<TimerEntry>> wheel_;
  public:
      int  add(int delay_ms, function<void()> cb);
      void tick();  // 每个 tick 调用一次，执行当前槽定时器
  };
  ```

**完成标志：** 时间轮设计图绘制完成，能解释与最小堆的区别

---
## 实践落点

- 代码目录：`CPP-Practice/high_perf_net/`
- 核心文件：`CPP-Practice/high_perf_net/mini_high_perf_net.h`
- 验证入口：`CPP-Practice/high_perf_net/main.cpp`
- 覆盖主题：TimerWheel、连接空闲超时、PrecisionTimerHeap

## 验证命令

```powershell
cmake -S CPP-Practice/high_perf_net -B CPP-Practice/high_perf_net/build
cmake --build CPP-Practice/high_perf_net/build --config Release
.\CPP-Practice\high_perf_net\build\Release\high_perf_net_demo.exe
```

