## Day 142（Tue）— 格式化（时间戳 + 线程 ID）

**预计时间：1 小时**

**任务：**
- [x] 实现日志格式：`[2024-01-15 10:30:45.123] [INFO] [TID:12345] [main.cpp:42] message`
  ```cpp
  // 时间戳：用 gettimeofday / clock_gettime 获取毫秒级时间
  // 线程 ID：syscall(SYS_gettid) 或 pthread_self()
  // 格式化到栈缓冲区（避免 heap 分配）：
  char buf[256];
  snprintf(buf, sizeof(buf), "[%s] [%s] [TID:%lu] [%s:%d] ",
           time_str, level_str, tid, file, line);
  ```
- [x] 实现文件名截短（只取 `__FILE__` 的最后一段，去掉路径前缀）
- [x] 测试：多线程同时输出，TID 不同

**完成标志：** 日志格式正确，多线程不串行

---
## 实践落点

- 代码目录：`CPP-Practice/async_logger/`
- 核心文件：`CPP-Practice/async_logger/mini_logger.h`
- 验证入口：`CPP-Practice/async_logger/main.cpp`
- 覆盖主题：SyncLogger、日志级别、格式化、线程安全写入、流式接口

## 验证命令

```powershell
cmake -S CPP-Practice/async_logger -B CPP-Practice/async_logger/build
cmake --build CPP-Practice/async_logger/build --config Release
.\CPP-Practice\async_logger\build\Release\async_logger_demo.exe
```

