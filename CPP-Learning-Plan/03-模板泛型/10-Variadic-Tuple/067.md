## Day 67（Thu）— 手写 get<N>

**预计时间：1 小时**

**任务：**
- [x] 实现 `get<N>(tuple)`：
  ```cpp
  // 辅助：获取第 N 层基类的类型
  template<size_t N, typename Head, typename... Tail>
  struct TupleElement {
      using type = typename TupleElement<N-1, Tail...>::type;
  };
  template<typename Head, typename... Tail>
  struct TupleElement<0, Head, Tail...> {
      using type = Head;
  };

  // get 函数：通过向上转型到第 N 层基类
  template<size_t N, typename... Ts>
  auto& get(Tuple<Ts...>& t) {
      using Base = Tuple</* 第 N 层 */ ...>;
      return static_cast<Base&>(t).value;
  }
  ```
- [x] 测试：`get<0>(t) == 1`，`get<2>(t) == "hello"`

**完成标志：** get<N> 能正确获取对应元素，返回引用（可修改）

## 实践落点

- 头文件：`CPP-Practice/variadic_tuple/templates/my_tuple.h`
- 相关实现：`TupleElement<N, Ts...>`、`tuple_node_t<N, Ts...>`、`get<N>()`

## 本日结论

- `get<N>` 的关键不是运行时查找，而是在编译期找到第 N 层 `Tuple<Head, Tail...>` 节点
- 通过 `static_cast<Node&>(tuple).value` 可以定位对应层的元素
- 返回引用后可以直接修改 tuple 内部元素，这是本日完成标志的重要部分

---