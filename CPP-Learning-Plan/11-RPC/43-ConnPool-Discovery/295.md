## Day 295（Mon）— RPC 连接池集成

**预计时间：1 小时**

**任务：**
- [x] 将 Month 9 的 `TcpConnPool` 接入 `AsyncRpcClient`：
  ```cpp
  class PooledRpcClient {
      LoadBalancer balancer_;  // 持有多后端连接池
  public:
      Future<RpcResponse> call(const string& svc, const string& method,
                                const Message& req) {
          auto conn = balancer_.acquire();        // 从池中取连接
          return async_call(conn, svc, method, req)
              .then([conn, &balancer_](auto resp){
                  balancer_.release(conn);        // 用完归还
                  return resp;
              });
      }
  };
  ```
- [x] 测试：8 线程并发 RPC，连接池大小 16，验证连接复用

> 口径说明：本仓库落点为跨平台模拟（rpc_framework 仅提交内存版 `Registry` + `RpcClient`，`TcpConnPool`/`LoadBalancer`/`PooledRpcClient` 未落地，所依赖的连接池为 Month 9 进程内模拟，非真实 TCP），本日 [x] 记录的是连接池与 RPC client 衔接的概念与设计掌握；面向真实 TCP 的连接池化 RPC client 属于下一步 Linux 补课，见本周 README 阶段小结（真实 epoll+timerfd 落地参考 raft_kv/net/）。

**完成标志：** 连接池与 RPC 框架集成，并发调用无报错

---
## 实践落点

- 代码目录：`CPP-Practice/rpc_framework/`
- 核心文件：`CPP-Practice/rpc_framework/mini_rpc.h`
- 验证入口：`CPP-Practice/rpc_framework/main.cpp`
- 覆盖主题：Registry 服务发现、RoundRobin endpoint、连接池语义衔接

## 验证命令

```powershell
cmake -S CPP-Practice/rpc_framework -B CPP-Practice/rpc_framework/build
cmake --build CPP-Practice/rpc_framework/build --config Release
.\CPP-Practice\rpc_framework\build\Release\rpc_framework_demo.exe
```

