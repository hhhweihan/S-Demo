## Day 224（Sun）— 复盘 + 规划 Month 9

**预计时间：1 小时**

- [x] 整理 Reactor 模式的完整设计图（EventLoop/Channel/Poller/TcpConnection）
- [ ] `git commit`，推送

---

## 本周结果（完成后填写）

**跨平台功能验证：**
| 场景 | 结果 | 备注 |
|------|------|------|
| Buffer 粘包/半包处理 | 通过 | 长度字段帧可等待完整包并连续解包 |
| 单线程 Reactor 抽象 | 通过 | FakePoller 激活 Channel 后 EventLoop 分发 read callback |
| one-loop-per-thread 模型 | 通过 | EventLoopThreadPool 轮转分配 loop |
| HTTP GET | 通过 | `HttpServerCore` 返回 200 和 handler body |
| RPC 往返 | 通过 | 长度字段 frame 上完成 echo 请求-响应 |
| wrk/QPS 压测 | 未执行 | 需要 Linux/WSL2 + 真实 socket server |
## 实践落点

- 代码目录：`CPP-Practice/network_reactor/`
- 核心文件：`CPP-Practice/network_reactor/mini_reactor.h`
- 验证入口：`CPP-Practice/network_reactor/main.cpp`
- 覆盖主题：HTTP GET 响应、RPC frame 往返、整月网络编程复盘

## 验证命令

```powershell
cmake -S CPP-Practice/network_reactor -B CPP-Practice/network_reactor/build
cmake --build CPP-Practice/network_reactor/build --config Release
.\CPP-Practice\network_reactor\build\Release\network_reactor_demo.exe
```

