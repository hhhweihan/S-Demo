## Day 200（Thu）— select 多路复用（了解）

**预计时间：1 小时**

**任务：**
- [ ] 用 `select` 监听多个 fd（最多 FD_SETSIZE = 1024）
- [ ] 实现能处理 10 个并发客户端的 Echo Server
- [ ] 理解 select 的局限性（O(n) 扫描，fd 上限），为 epoll 做铺垫

**完成标志：** 10 个并发客户端都能正确 echo

---