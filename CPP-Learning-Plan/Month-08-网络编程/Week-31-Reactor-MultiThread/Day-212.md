## Day 212（Tue）— TcpConnection

**预计时间：1 小时**

**任务：**
- [ ] 实现 `TcpConnection`（用 shared_ptr 管理生命周期）：
  ```cpp
  class TcpConnection : public std::enable_shared_from_this<TcpConnection> {
      Channel channel_;
      Buffer input_buf_, output_buf_;
      std::function<void(TcpConnectionPtr, Buffer*)> message_cb_;
  public:
      void send(std::string data);   // 线程安全：通过 EventLoop 调度
      void shutdown();
  private:
      void handle_read();
      void handle_write();
  };
  ```

**完成标志：** TcpConnection 能收发数据，生命周期由 shared_ptr 管理

---