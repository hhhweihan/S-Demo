## Day 215（Fri）— 完整 HTTP Server + wrk 压测

**预计时间：1 小时**

**任务：**
- [ ] 整合所有组件成一个 HTTP Server：
  - `GET /` 返回 "Hello World"
  - `GET /stats` 返回当前连接数和请求计数
- [ ] 用 `wrk` 压测（在另一台机器或 WSL 里）：
  ```bash
  wrk -t4 -c100 -d10s http://127.0.0.1:8080/
  ```
- [ ] 记录 QPS（目标 5000+）

**完成标志：** QPS > 5000，无崩溃，无内存泄漏

---