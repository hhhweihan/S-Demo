## Day 183（Mon）— 读 MemTable

**预计时间：1 小时**

**任务：**
- [ ] 读 `db/memtable.h` + `db/memtable.cc`（约 130 行）
- [ ] 理解 MemTable 内部 key 的编码格式：
  ```
  InternalKey = UserKey + SequenceNumber(7字节) + ValueType(1字节)
  ValueType: kTypeValue(put) or kTypeDeletion(delete)
  ```
- [ ] 回答：
  1. 为什么 delete 操作不立刻删除，而是写一个墓碑标记（kTypeDeletion）？
  2. SequenceNumber 的作用是什么（MVCC 快照读）？
  3. MemTable 何时变成 Immutable？触发条件是什么（write_buffer_size）？

**完成标志：** 3 个问题有答案，理解 InternalKey 的编码

---