## Day 172（Thu）— 读 Slice + Status

**预计时间：1 小时**

**任务：**
- [ ] 读 `include/leveldb/slice.h`（约 80 行）：
  1. Slice 的本质：只是 `(const char*, size_t)` 的包装
  2. 为什么不用 `std::string`？（避免拷贝）
  3. Slice 和 C++17 `std::string_view` 的区别是什么？
- [ ] 读 `include/leveldb/status.h` + `util/status.cc`（约 100 行）：
  1. Status 如何在一个指针里编码：OK / error_code / error_msg？
  2. 为什么 LevelDB 不用异常，而用 Status 返回值？
  3. Status 复制时发生了什么（hint：`state_` 是 `const char*`）？

**完成标志：** 6 个问题都有答案

---