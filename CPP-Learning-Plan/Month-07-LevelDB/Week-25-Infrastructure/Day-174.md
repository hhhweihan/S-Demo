## Day 174（Sat）— 画整体架构图

**预计时间：2 小时**

**任务：**
- [ ] 读 `db/db_impl.h`（只读头文件，约 200 行）
- [ ] 结合 `doc/impl.md`，画出完整的架构图，包含：
  - 写入路径（从 `DB::Put` 到磁盘）
  - 读取路径（从 `DB::Get` 到返回值）
  - 后台 Compaction 触发条件
  - 各组件的生命周期（谁持有谁）
- [ ] 把图存入 Note 目录

**完成标志：** 图完整，包含写/读/compaction 三条路径

---