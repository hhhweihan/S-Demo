## Day 171（Wed）— 仿写 Arena

**预计时间：1 小时**

**任务：**
- [ ] 不看源码，从零实现简化版 `Arena`（只需实现 `Allocate` 和析构，不需要 `AllocateAligned`）
  ```cpp
  class Arena {
      std::vector<char*> blocks_;
      char* alloc_ptr_ = nullptr;
      size_t alloc_bytes_remaining_ = 0;
      static constexpr size_t kBlockSize = 4096;
  public:
      char* Allocate(size_t bytes);
      ~Arena();
  };
  ```
- [ ] 对比 LevelDB 源码：找出 2 处你的实现和原版的不同

**完成标志：** 仿写版编译通过，能正确分配内存，析构无泄漏

---