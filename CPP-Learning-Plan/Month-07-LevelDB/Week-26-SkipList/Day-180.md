## Day 180（Fri）— 性能对比

**预计时间：1 小时**

**任务：**
- [ ] 写 benchmark：插入 100 万个 int，查找 100 万次（50% 命中率）：
  ```
  你的 SkipList（有锁）
  你的 SkipList（无锁）
  std::map（红黑树）
  std::unordered_map（哈希表，仅作参考）
  ```
- [ ] 分析：为什么跳表在实际中比 std::map 更受青睐（提示：缓存友好性、并发友好性）？

**完成标志：** 有 benchmark 数据，有分析

---