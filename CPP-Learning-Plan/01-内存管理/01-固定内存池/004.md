## Day 4（Thu）— 单元测试

**预计时间：1 小时**

**任务（每条测试写一个函数）：**
- [x] **基础测试**：分配 1000 次，释放 1000 次，`stats.used == 0`
- [x] **重复利用测试**：分配 A，释放 A，再分配 B，验证 `A == B`（地址复用）
- [x] **扩展测试**：初始容量设为 4，分配 100 次，验证 `chunk` 数 > 1
- [x] **混合测试**：随机顺序分配/释放 10000 次，用 `set` 记录已分配指针，验证无重复分配
- [x] 使用自定义 `EXPECT_TRUE/EXPECT_EQ` 宏，失败时抛异常并输出失败原因

**本次实现说明：**
- 在 `CPP-Practice/memory_pool/main.cpp` 中拆分为 4 个测试函数：
	- `test_basic_allocate_deallocate()`
	- `test_reuse_address()`
	- `test_expand_chunks()`
	- `test_mixed_random_allocate_deallocate()`
- 混合测试使用 `std::unordered_set<void*>` 跟踪当前已分配指针，确保不会把同一块地址重复发给“仍在使用中的对象”。
- `main()` 统一捕获异常，任何断言失败都会返回非 0 退出码，便于后续接入 CI。

**运行结果：** `Day 004 unit tests passed`

**验证命令（Windows / VS Build Tools）：**
```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64 -HostArch amd64
Set-Location "d:\CodeSpace\S-Demo\CPP-Practice\memory_pool"
& "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" -S . -B build -G Ninja
& "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" --build build
.\build\memory_pool_demo.exe
```

**完成标志：** 所有测试通过，无段错误（已完成）

---