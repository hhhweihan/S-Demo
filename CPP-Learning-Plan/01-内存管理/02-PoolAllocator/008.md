## Day 8（Mon）— size class 映射设计

**预计时间：1 小时**

**任务：**
- [x] 创建 `pool_allocator.h`
- [x] 设计 size class：8, 16, 24, 32, 48, 64, 80, 96, 128, 192, 256, 384, 512, 768, 1024，超过 1024 走大对象 fallback
- [x] 实现 `size_to_class(size_t n)` 函数，返回小对象 class index，`>1024` 返回 `-1`
  ```cpp
  // 示例：n=10 → 对应 16 字节 class（index 1）
  // 用静态查表或简单的 if 链实现，不用过于精妙
  ```
- [x] 在 `main.cpp` 里打印 1-1200 字节各自对应的 class，验证映射正确

**完成标志：** 映射函数无越界，打印结果符合预期

**实现说明：**
- 这一天先把 PoolAllocator 的路由层搭起来，当前只实现 size class 映射，不急着接入 `FixedAllocator` 数组。
- 小对象使用 15 个固定档位，分别覆盖 `1..1024`；超过 `1024` 的请求统一返回 `-1`，留给 Day 10 的大对象 fallback。
- `main.cpp` 中加入了边界断言与 `1..1200` 的完整打印，便于肉眼检查每个区间是否落到正确的 class。

**本日结果：**
- `pool_allocator.h` 已创建
- `size_to_class` / `class_to_block_size` 已实现
- Day-008 映射测试已加入 `main.cpp`
- 输出会显示 `1..1024` 对应的 class 与 block size，`1025..1200` 标记为 `large`

---