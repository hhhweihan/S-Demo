## Day 150（Wed）— 后台线程写入逻辑

**预计时间：1 小时**

**任务：**
- [x] 实现后台线程 `backend_thread()`：
  ```cpp
  void AsyncLogger::backend_thread() {
      while (running_) {
          {
              std::unique_lock lock(mu_);
              cv_.wait_for(lock, 3s, [this]{ return !buffers_.empty(); });
              buffers_.push_back(std::move(current_));
              current_ = std::make_unique<Buffer>();
          }
          // 在锁外批量写入文件（IO 不持锁）
          for (auto& buf : buffers_to_write)
              file_.write(buf->data(), buf->used());
          file_.flush();
          buffers_to_write.clear();
      }
  }
  ```
- [x] 测试：前台 4 线程连续写，后台线程正确 flush

**完成标志：** 无日志丢失，后台线程定时 flush 有效

---
## 实践落点

- 代码目录：`CPP-Practice/async_logger/`
- 核心文件：`CPP-Practice/async_logger/mini_logger.h`
- 验证入口：`CPP-Practice/async_logger/main.cpp`
- 覆盖主题：AsyncLogger、前台缓冲、后台线程、条件变量、批量 flush

## 验证命令

```powershell
cmake -S CPP-Practice/async_logger -B CPP-Practice/async_logger/build
cmake --build CPP-Practice/async_logger/build --config Release
.\CPP-Practice\async_logger\build\Release\async_logger_demo.exe
```

