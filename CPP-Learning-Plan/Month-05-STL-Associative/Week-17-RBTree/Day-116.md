## Day 116（Thu）— 中序遍历迭代器

**预计时间：1 小时**

**任务：**
- [ ] 实现 `RBTree::iterator`（双向迭代器，中序遍历）：
  ```cpp
  // operator++：找右子树的最左节点；若无右子树，向上找第一个「从左子到达的祖先」
  // operator--：找左子树的最右节点；若无左子树，向上找第一个「从右子到达的祖先」
  // 使用哨兵节点 end_（指向 header，简化边界处理）
  ```
- [ ] 测试：`for (auto it = tree.begin(); it != tree.end(); ++it)` 输出有序序列

**完成标志：** 中序遍历输出有序，`--` 从 end() 能正确到最大节点

---

