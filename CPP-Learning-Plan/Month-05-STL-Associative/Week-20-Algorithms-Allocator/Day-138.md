## Day 138（Fri）— 整月综合 benchmark

**预计时间：1 小时**

**任务：**
- [x] 跑完整 benchmark（各 100 万次操作）：
  ```
  MyVector push_back      vs std::vector
  MyList   push_back      vs std::list
  MyMap    insert/find    vs std::map
  MyUnorderedMap insert   vs std::unordered_map
  MyVector + PoolAllocator vs MyVector + std::allocator
  ```
- [x] 用 ASan + Valgrind（如果 Linux）检测内存泄漏
- [x] 验证月度验收标准：
  - [x] 红黑树 10 万数后树高 ≤ 2×log₂(n)
  - [x] MyMap 支持 lower_bound/upper_bound
  - [x] MyUnorderedMap 在 load_factor 0.75 自动 rehash
  - [ ] PoolAllocator 版本比默认快 20%+

**完成标志：** 所有验收标准达成，数据已记录

---
## 实践落点

- 代码目录：`CPP-Practice/stl_associative/`
- 核心文件：`CPP-Practice/stl_associative/mini_associative.h`
- 验证入口：`CPP-Practice/stl_associative/main.cpp`
- 覆盖主题：heap_sort、stable_sort、partial_sort、binary_search、PoolAllocator allocator_traits 接入

## 验证命令

```powershell
cmake -S CPP-Practice/stl_associative -B CPP-Practice/stl_associative/build
cmake --build CPP-Practice/stl_associative/build --config Release
.\CPP-Practice\stl_associative\build\Release\stl_associative_demo.exe
```

