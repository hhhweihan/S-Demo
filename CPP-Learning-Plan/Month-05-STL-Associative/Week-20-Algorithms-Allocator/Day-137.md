## Day 137（Thu）— allocator_traits 完整接入

**预计时间：1 小时**

**任务：**
- [x] 将 Month 4-5 所有容器改为通过 `allocator_traits` 调用分配器：
  ```cpp
  // Before（直接调用 allocator）：
  alloc_.construct(ptr, args...);
  // After（通过 allocator_traits）：
  allocator_traits<Alloc>::construct(alloc_, ptr, args...);
  ```
- [x] 接入 Month 1 的 PoolAllocator：
  ```cpp
  MyVector<int, PoolAllocator<int>> pv;
  MyList<int, PoolAllocator<int>>   pl;
  MyMap<int, int, std::less<int>, PoolAllocator<pair<const int,int>>> pm;
  ```
- [x] 验证：所有容器用 PoolAllocator 能正常工作（ASan 无报错）

**完成标志：** PoolAllocator 在 MyVector/MyList/MyMap 上均正常

---
## 实践落点

- 代码目录：`CPP-Practice/stl_associative/`
- 核心文件：`CPP-Practice/stl_associative/mini_associative.h`
- 验证入口：`CPP-Practice/stl_associative/main.cpp`
- 覆盖主题：heap_sort、stable_sort、partial_sort、binary_search、PoolAllocator allocator_traits 接入

## 验证命令

```powershell
cmake -S CPP-Practice/stl_associative -B CPP-Practice/stl_associative/build
cmake --build CPP-Practice/stl_associative/build --config Release
.\CPP-Practice\stl_associative\build\Release\stl_associative_demo.exe
```

