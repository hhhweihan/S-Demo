## Day 31（Wed）— 生产者消费者测试

**预计时间：1 小时**

**任务：**
- [ ] 写 `test_blocking_queue.cpp`：
  - 4 个生产者线程，各 push 25000 个数字（共 100000 个）
  - 4 个消费者线程，各 pop 并累加
  - 主线程等所有线程结束后，验证：`总和 == 0+1+2+...+99999`
- [ ] 用 `std::atomic<int>` 记录已 push 数量，防止多 push 了
- [ ] 测试用 ASan + TSan 编译运行：`-fsanitize=address,thread`

**完成标志：** 总和正确，TSan 无报错

---