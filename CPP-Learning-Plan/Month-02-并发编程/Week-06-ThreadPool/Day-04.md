## Day 4（Thu）— 动态扩缩容

**预计时间：1 小时**

**任务：**
- [ ] 添加最小/最大线程数配置
- [ ] 每隔 1 秒检查一次队列长度：
  - 队列长度 > 阈值且当前线程数 < max：新增一个 worker
  - 队列空且当前线程数 > min：标记一个 worker 退出
- [ ] 用 `std::atomic<size_t> active_count_` 记录活跃线程数
- [ ] 测试：初始 2 个线程，提交 100 个慢任务（sleep 10ms），观察线程数增长

**完成标志：** 线程数在最小和最大之间动态变化

---