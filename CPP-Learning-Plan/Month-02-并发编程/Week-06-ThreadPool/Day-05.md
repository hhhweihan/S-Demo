## Day 5（Fri）— 压力测试

**预计时间：1 小时**

**任务：**
- [ ] 提交 **100 万个**轻量任务（仅做 `++counter`），计时：
  ```
  4 线程线程池：总耗时 / QPS
  8 线程线程池：总耗时 / QPS
  单线程直接执行：总耗时（基准）
  ```
- [ ] 用 `std::atomic<int> counter` 验证 100 万次全部执行（无丢失）
- [ ] 填写「本周结果」

**完成标志：** 无任务丢失，有 QPS 数据

---