## Day 43（Mon）— memory_order 实验

**预计时间：1 小时**

**任务：**
- [ ] 写 `lockfree/memory_order_test.cpp`，用代码感受 6 种 order 的区别：
  ```cpp
  // 实验 1：relaxed（无同步语义）
  std::atomic<int> x{0}, y{0};
  // 线程 A：x.store(1, relaxed); y.store(1, relaxed);
  // 线程 B：while(!y.load(relaxed)); assert(x.load(relaxed) == 1);  // 可能失败！

  // 实验 2：release/acquire 对（有同步语义）
  // 线程 A：x.store(1, relaxed); y.store(1, release);
  // 线程 B：while(!y.load(acquire)); assert(x.load(relaxed) == 1);  // 保证成功
  ```
- [ ] 用 TSan 运行实验 1，观察是否报数据竞争
- [ ] 整理 6 种 memory_order 的一句话定义（写在「本周结果」）

**完成标志：** 能区分 relaxed / acquire / release / acq_rel / seq_cst

---