## Day 71（Mon）— 手写 optional

**预计时间：1 小时**

**任务：**
- [x] 创建 `templates/my_optional.h`
- [x] 实现 `Optional<T>`：
  ```cpp
  template<typename T>
  class Optional {
      alignas(T) char storage_[sizeof(T)];  // 对齐存储，不提前构造
      bool has_value_ = false;
  public:
      Optional() = default;
      Optional(T val) : has_value_(true) { new(storage_) T(std::move(val)); }
      ~Optional() { if (has_value_) reinterpret_cast<T*>(storage_)->~T(); }
      // 拷贝/移动构造、赋值...
      bool has_value() const { return has_value_; }
      T& value();                      // 无值时抛 std::bad_optional_access
      T value_or(T default_val) const;
      explicit operator bool() const { return has_value_; }
  };
  ```
- [x] 测试：Optional<std::string> 无值 / 有值 / value_or

**完成标志：** 无值时 value() 抛异常，有值时正确返回

## 实践落点

- 代码目录：`CPP-Practice/type_erasure/`
- 头文件：`CPP-Practice/type_erasure/templates/my_optional.h`
- 验证入口：`CPP-Practice/type_erasure/main.cpp` 的 `run_optional_examples()`

## 本日结论

- `Optional<T>` 的核心是“有存储但不一定构造对象”
- `std::aligned_storage_t` 提供对齐存储，placement new 负责按需构造
- `value()` 必须在无值时抛 `std::bad_optional_access`，否则调用者无法区分空状态和默认值

---