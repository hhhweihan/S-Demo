## Day 69（Sat）— 读 libc++ tuple 源码

**预计时间：2 小时**

**任务：**
- [ ] 找到 libc++ `include/tuple`
- [ ] 对比你的实现，回答：
  1. libc++ 为什么用 `__tuple_leaf` 而不是像你一样直接继承？（EBO 优化）
  2. libc++ 的 `tuple_element` 和你的 `TupleElement` 有什么区别？
  3. libc++ 的 `tuple_cat` 是如何把两个 tuple 合并的？（你不需要实现，只需要理解思路）

**完成标志：** 3 个问题有答案

---