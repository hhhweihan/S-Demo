## Day 60（Thu）— 手写 type_traits（一）

**预计时间：1 小时**

**任务：**
- [ ] 创建 `templates/my_type_traits.h`，从零实现：
  - `remove_const<T>` / `remove_reference<T>` / `remove_cv<T>`
  - `add_pointer<T>` / `add_lvalue_reference<T>`
  - `is_pointer<T>` / `is_reference<T>` / `is_array<T>`
  - `conditional<B, T, F>`：B 为 true 时 type=T，否则 type=F
- [ ] 每个实现 10 行以内，主要是模板特化
- [ ] 用 `static_assert` 验证所有实现正确

**完成标志：** 所有 static_assert 通过

---