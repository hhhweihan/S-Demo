## Day 59（Wed）— enable_if 条件编译

**预计时间：1 小时**

**任务：**
- [ ] 手写 `enable_if`：
  ```cpp
  template<bool B, typename T = void>
  struct enable_if {};
  template<typename T>
  struct enable_if<true, T> { using type = T; };
  ```
- [ ] 用 `enable_if` 限制模板：
  ```cpp
  // 只有 T 是整数类型时才能调用
  template<typename T>
  typename std::enable_if<std::is_integral<T>::value, T>::type
  double_it(T x) { return x * 2; }
  ```
- [ ] 用 C++17 `if constexpr` 重写同样的逻辑，对比代码量

**完成标志：** `double_it(3)` 编译通过，`double_it(3.14)` 编译报错

---