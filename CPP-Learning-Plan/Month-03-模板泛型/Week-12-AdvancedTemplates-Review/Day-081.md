## Day 81（Thu）— 编译期计算（constexpr）

**预计时间：1 小时**

**任务：**
- [x] 用 `constexpr` 实现编译期：
  - 斐波那契数列
  - 快速排序（C++20 constexpr vector 或递归数组）
  - 质数筛（Sieve of Eratosthenes）
- [x] 用 `static_assert` 验证编译期结果
- [x] 实现编译期字符串哈希：
  ```cpp
  constexpr size_t hash(const char* s) { ... }
  static_assert(hash("hello") != hash("world"));
  ```

**完成标志：** 所有结果在编译期确定，static_assert 全部通过

## 实践落点

- 代码目录：`CPP-Practice/advanced_templates/`
- 相关函数：`fibonacci()`、`constexpr_sort()`、`prime_sieve()`、`hash()`

## 本日结论

- C++20 的 `constexpr` 已经能承载较复杂的循环和数组处理
- `static_assert` 是确认结果发生在编译期的最直接方式
- 编译期字符串哈希适合做轻量分发、日志 tag 或协议字段预计算，但仍要注意哈希冲突

---