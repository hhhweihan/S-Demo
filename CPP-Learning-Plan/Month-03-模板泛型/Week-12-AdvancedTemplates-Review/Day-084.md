## Day 84（Sun）— 规划 Month 4

**预计时间：1 小时**

- [x] clone LevelDB：`git clone https://github.com/google/leveldb`
- [x] 用 CMake 编译，运行 `db_bench` 看基准数字
- [x] 阅读 `Month-04-LevelDB/README.md`（当前仓库实际 LevelDB 月计划位于 `Month-07-LevelDB/README.md`，Month 4 当前是 STL 序列容器）
- [ ] `git commit`，推送（需要用户明确确认后再执行）

## 实践落点

- LevelDB 克隆目录：`TestProject/leveldb/`
- 构建目录：`TestProject/leveldb/build/`
- 可执行文件：`TestProject/leveldb/build/Release/db_bench.exe`

## db_bench 小规模结果

命令：

```powershell
.\TestProject\leveldb\build\Release\db_bench.exe --benchmarks=fillseq,readrandom --num=10000 --reads=10000
```

结果：

- `fillseq`: `11.921 micros/op`, `9.3 MB/s`
- `readrandom`: `0.808 micros/op`, `10000 of 10000 found`
- 当前 Windows 构建未启用 Snappy，输出中有 `WARNING: Snappy compression is not enabled`

## 本日结论

- 题面提到的 Month04-LevelDB 与当前仓库结构不一致；实际 Month04 是 STL 序列容器，LevelDB 在 Month07
- 已按题面完成 LevelDB clone、子模块初始化、`db_bench` 构建和小规模运行
- 后续正式进入 Month04 时，应优先阅读 `CPP-Learning-Plan/Month-04-STL-Sequence/README.md`