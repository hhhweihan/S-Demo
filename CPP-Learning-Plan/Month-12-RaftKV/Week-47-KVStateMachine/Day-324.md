## Day 324（Tue）— Put / Get / Delete

**预计时间：1 小时**

**任务：**
- [ ] 实现 `KVStateMachine`（内存版，先不用 LevelDB）：
  ```cpp
  class KVStateMachine : public StateMachine {
      std::unordered_map<std::string, std::string> store_;
      std::mutex mu_;
  public:
      void apply(const std::string& cmd) override;
      std::string get(const std::string& key) override;
  };
  ```
- [ ] 实现客户端接口：
  - 客户端 → Leader（HTTP 或 TCP）→ `Propose(command)` → 等待 commit → 返回结果
  - `Propose`：把命令作为新 log entry 追加，等到该 index 被 commit

**完成标志：** 客户端能通过 Leader Put/Get，结果正确

---