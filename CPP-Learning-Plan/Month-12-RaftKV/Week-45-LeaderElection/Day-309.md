## Day 309（Mon）— 项目骨架 + 消息格式

**预计时间：1 小时**

**任务：**
- [x] 建 `raft_kv/` 项目目录，初始化 CMakeLists.txt
- [x] 定义状态和消息类型（用 Month 5 写好的 protobuf）：
  ```
  enum Role { FOLLOWER, CANDIDATE, LEADER }
  消息：RequestVoteRequest / RequestVoteResponse
       AppendEntriesRequest / AppendEntriesResponse（心跳用空 entries）
  ```
- [x] 定义 `RaftNode` 的核心状态：
  ```cpp
  struct RaftState {
      int64_t current_term = 0;
      int32_t voted_for = -1;     // 本 term 投票给谁（-1 表示未投）
      Role role = FOLLOWER;
      int32_t node_id;
      int32_t leader_id = -1;
  };
  ```

**完成标志：** 项目能编译，结构清晰

---
## 实践落点

- 代码目录：`CPP-Practice/raft_kv/`
- 核心文件：`CPP-Practice/raft_kv/mini_raft_kv.h`
- 验证入口：`CPP-Practice/raft_kv/main.cpp`
- 覆盖主题：RaftNode 角色状态、term、RequestVote、多数派选举

## 验证命令

```powershell
cmake -S CPP-Practice/raft_kv -B CPP-Practice/raft_kv/build
cmake --build CPP-Practice/raft_kv/build --config Release
.\CPP-Practice\raft_kv\build\Release\raft_kv_demo.exe
```

