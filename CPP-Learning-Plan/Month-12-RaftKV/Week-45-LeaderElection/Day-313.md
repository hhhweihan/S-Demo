## Day 313（Fri）— 心跳处理

**预计时间：1 小时**

**任务：**
- [x] Follower 收到 AppendEntries（心跳）：
  ```cpp
  // 重置选举计时器
  // 如果 req.term >= current_term，承认对方是 Leader，变为 Follower
  // 回复 AppendEntriesResponse{term=current_term, success=true}
  ```
- [x] Leader 收到 AppendEntriesResponse：
  - 如果 resp.term > current_term → 变为 Follower（发现更新的 term）

**完成标志：** 心跳正常循环，Follower 不再超时

---
## 实践落点

- 代码目录：`CPP-Practice/raft_kv/`
- 核心文件：`CPP-Practice/raft_kv/mini_raft_kv.h`
- 验证入口：`CPP-Practice/raft_kv/main.cpp`
- 覆盖主题：RaftNode 角色状态、term、RequestVote、多数派选举

## 验证命令

```powershell
cmake -S CPP-Practice/raft_kv -B CPP-Practice/raft_kv/build
cmake --build CPP-Practice/raft_kv/build --config Release
.\CPP-Practice\raft_kv\build\Release\raft_kv_demo.exe
```

