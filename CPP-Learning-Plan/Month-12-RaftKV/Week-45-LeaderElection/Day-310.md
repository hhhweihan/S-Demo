## Day 310（Tue）— 选举超时计时器

**预计时间：1 小时**

**任务：**
- [ ] 实现 `ElectionTimer`：
  ```cpp
  class ElectionTimer {
      EventLoop* loop_;
      int timeout_ms_;    // 随机：150-300ms
      TimerId timer_id_;
      std::function<void()> timeout_cb_;
  public:
      void reset();       // 重置（收到心跳时调用）
      void start();
      void stop();
  };
  ```
- [ ] 选举超时是随机的（150-300ms），避免所有节点同时超时
- [ ] 测试：启动一个 Follower，不发任何消息，500ms 内触发超时

**完成标志：** 超时回调在 150-300ms 内触发

---