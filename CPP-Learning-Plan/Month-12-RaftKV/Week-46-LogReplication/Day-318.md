## Day 318（Wed）— Follower 处理 AppendEntries

**预计时间：1 小时**

**任务：**
- [ ] Follower 一致性检查：
  ```cpp
  // 检查 prev_index 和 prev_term 是否匹配
  if (log_.get(req.prev_log_index).term != req.prev_log_term) {
      return {current_term_, false};  // 拒绝，让 Leader 回退 next_index
  }
  // 追加条目（先 truncate 冲突部分）
  log_.truncate_after(req.prev_log_index);
  for (auto& e : req.entries) log_.append(e);
  // 更新 commit_index
  commit_index_ = std::min(req.leader_commit, log_.last_index());
  return {current_term_, true};
  ```

**完成标志：** Follower 能正确追加日志，拒绝不一致的请求

---