## Day 109（Thu）— allocator_traits 基础

**预计时间：1 小时**

**任务：**
- [x] 了解 `allocator_traits<Alloc>` 的作用：统一分配器接口
  ```cpp
  // 不管 Alloc 是否有 construct/destroy，都通过 allocator_traits 调用
  allocator_traits<Alloc>::construct(alloc, ptr, args...);
  allocator_traits<Alloc>::destroy(alloc, ptr);
  ```
- [x] 给 MyVector 和 MyList 改用 `allocator_traits` 而不是直接调用 `alloc_.construct()`
- [x] 测试：自定义一个只有 `allocate/deallocate` 的最简 Allocator，验证 allocator_traits 能自动补全

**完成标志：** 最简 Allocator 能在 MyVector 中工作

---
## 实践落点

- 代码目录：`CPP-Practice/stl_sequence/`
- 核心文件：`CPP-Practice/stl_sequence/mini_stl.h`
- 验证入口：`CPP-Practice/stl_sequence/main.cpp`
- 覆盖主题：MyStack、MyQueue、MyPriorityQueue 与整月综合验证

## 验证命令

```powershell
cmake -S CPP-Practice/stl_sequence -B CPP-Practice/stl_sequence/build
cmake --build CPP-Practice/stl_sequence/build --config Release
.\CPP-Practice\stl_sequence\build\Release\stl_sequence_demo.exe
```

