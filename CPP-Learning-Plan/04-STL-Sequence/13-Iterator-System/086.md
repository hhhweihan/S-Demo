## Day 86（Tue）— advance 和 distance 的高效实现

**预计时间：1 小时**

**任务：**
- [x] 实现 `advance(it, n)`：根据迭代器类别选择最优实现
  ```cpp
  // random_access: it += n;  O(1)
  // bidirectional: 正负方向各自循环  O(n)
  // input/forward: 只能正向循环  O(n)
  // 用 tag dispatch 而非 if constexpr（先练习 tag dispatch）
  template<typename Iter>
  void advance(Iter& it, typename iterator_traits<Iter>::difference_type n) {
      advance_impl(it, n, typename iterator_traits<Iter>::iterator_category{});
  }
  ```
- [x] 实现 `distance(first, last)`：同样 tag dispatch
- [x] 测试：对 `int*`（random_access）、`std::list<int>::iterator`（bidirectional）分别计时

**完成标志：** random_access 版本 O(1)，bidirectional 版本 O(n)，结果正确

---
## 实践落点

- 代码目录：`CPP-Practice/stl_sequence/`
- 核心文件：`CPP-Practice/stl_sequence/mini_stl.h`
- 验证入口：`CPP-Practice/stl_sequence/main.cpp`
- 覆盖主题：迭代器体系、iterator_traits、ReverseIterator

## 验证命令

```powershell
cmake -S CPP-Practice/stl_sequence -B CPP-Practice/stl_sequence/build
cmake --build CPP-Practice/stl_sequence/build --config Release
.\CPP-Practice\stl_sequence\build\Release\stl_sequence_demo.exe
```

