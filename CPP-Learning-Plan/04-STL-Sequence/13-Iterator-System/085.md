## Day 85（Mon）— 迭代器类别标签

**预计时间：1 小时**

**任务：**
- [x] 创建 `mystl/iterator.h`，定义 5 种迭代器类别标签（空结构体）：
  ```cpp
  struct input_iterator_tag {};
  struct output_iterator_tag {};
  struct forward_iterator_tag       : input_iterator_tag {};
  struct bidirectional_iterator_tag : forward_iterator_tag {};
  struct random_access_iterator_tag : bidirectional_iterator_tag {};
  ```
- [x] 定义 `iterator_traits<Iterator>` 主模板和针对裸指针的偏特化：
  ```cpp
  template<typename Iter>
  struct iterator_traits {
      using iterator_category = typename Iter::iterator_category;
      using value_type        = typename Iter::value_type;
      using difference_type   = typename Iter::difference_type;
      using pointer           = typename Iter::pointer;
      using reference         = typename Iter::reference;
  };
  // 裸指针特化
  template<typename T>
  struct iterator_traits<T*> {
      using iterator_category = random_access_iterator_tag;
      using value_type        = T;
      using difference_type   = ptrdiff_t;
      using pointer           = T*;
      using reference         = T&;
  };
  ```
- [x] 测试：`iterator_traits<int*>::iterator_category` 是 `random_access_iterator_tag`

**完成标志：** iterator_traits 针对裸指针和自定义迭代器都能正确推导

---
## 实践落点

- 代码目录：`CPP-Practice/stl_sequence/`
- 核心文件：`CPP-Practice/stl_sequence/mini_stl.h`
- 验证入口：`CPP-Practice/stl_sequence/main.cpp`
- 覆盖主题：迭代器体系、iterator_traits、ReverseIterator

## 验证命令

```powershell
cmake -S CPP-Practice/stl_sequence -B CPP-Practice/stl_sequence/build
cmake --build CPP-Practice/stl_sequence/build --config Release
.\CPP-Practice\stl_sequence\build\Release\stl_sequence_demo.exe
```

