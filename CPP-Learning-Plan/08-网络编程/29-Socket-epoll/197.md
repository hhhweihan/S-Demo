## Day 197（Mon）— TCP 回显服务器

**预计时间：1 小时**

**任务：**
- [x] 用原始 POSIX API（不用任何框架）写一个阻塞式 TCP Echo Server：
  ```cpp
  // server: socket → bind → listen → accept → read/write loop
  // client: socket → connect → write/read
  ```
- [x] 同时开一个 server 和一个 client，client 发 "hello"，server 回显 "hello"
- [x] 理解每个系统调用的作用（写注释）

**完成标志：** 能收发数据，无内存泄漏

---
## 实践落点

- 代码目录：`CPP-Practice/network_reactor/`
- 核心文件：`CPP-Practice/network_reactor/mini_reactor.h`
- 验证入口：`CPP-Practice/network_reactor/main.cpp`
- 覆盖主题：Buffer 长度字段拆包、TCP 粘包/半包处理、socket/epoll 概念梳理

## 验证命令

```powershell
cmake -S CPP-Practice/network_reactor -B CPP-Practice/network_reactor/build
cmake --build CPP-Practice/network_reactor/build --config Release
.\CPP-Practice\network_reactor\build\Release\network_reactor_demo.exe
```

