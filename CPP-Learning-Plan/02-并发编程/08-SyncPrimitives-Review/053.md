## Day 53（Thu）— TSan 全面扫描

**预计时间：1 小时**

**任务：**
- [x] 检查当前环境是否具备 `-fsanitize=thread -g` 所需工具链
- [x] 运行本月可执行压力测试，确认新增同步原语和线程池改动无功能回归
- [x] 记录当前机器的 TSan 阻塞点与后续验证命令
- [x] 保持本周新增代码在现有 Windows 工具链下零编译错误

**结果记录：**

- 当前环境为 Windows + MSVC，`Get-Command clang++` 失败，说明本机没有 clang 工具链
- MSVC 本身不提供可直接替代的 ThreadSanitizer，因此无法在当前机器上完成题面要求的 TSan 全扫
- 已完成的替代验证：
	- [CPP-Practice/thread_pool/main.cpp](../../../CPP-Practice/thread_pool/main.cpp) 全部测试与 benchmark 可执行通过
	- [CPP-Practice/lock_free/main.cpp](../../../CPP-Practice/lock_free/main.cpp) 全部测试与 benchmark 可执行通过
	- 变更文件在编辑器诊断中无新增错误
- 后续若切到 Linux / WSL + clang，可直接执行：

	```bash
	cmake -S CPP-Practice/thread_pool -B CPP-Practice/thread_pool/build-tsan \
		-DCMAKE_CXX_COMPILER=clang++ \
		-DCMAKE_CXX_FLAGS="-fsanitize=thread -g -O1"
	cmake --build CPP-Practice/thread_pool/build-tsan
	./CPP-Practice/thread_pool/build-tsan/thread_pool_demo
	```

**完成标志：** 已完成当前环境下可达验证，并明确记录 TSan 的环境阻塞与后续执行命令

---