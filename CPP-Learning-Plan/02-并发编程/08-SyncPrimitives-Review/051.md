## Day 51（Tue）— CountDownLatch

**预计时间：1 小时**

**任务：**
- [x] 创建 `concurrent/countdown_latch.h`
- [x] 实现 `CountDownLatch`：
  ```cpp
  class CountDownLatch {
      std::mutex mtx_;
      std::condition_variable cv_;
      int count_;
  public:
      explicit CountDownLatch(int n) : count_(n) {}
      void count_down();        // --count_，归零时 notify_all
      void wait();              // 等待 count_ == 0
      void wait_for(std::chrono::milliseconds timeout); // 带超时
  };
  ```
- [x] 测试：主线程等待 5 个子任务全部完成（每个子任务 count_down() 一次）

**实现位置：**

- [CPP-Practice/thread_pool/concurrent/countdown_latch.h](../../../CPP-Practice/thread_pool/concurrent/countdown_latch.h)
- [CPP-Practice/thread_pool/main.cpp](../../../CPP-Practice/thread_pool/main.cpp)

**结果记录：**

- 实现除了题面要求的 `wait()` / `wait_for()` / `count_down()`，还补了 `count_up()`，便于 Day 52 接入线程池动态任务计数
- `test_day51_countdown_latch()` 先用 `wait_for(5ms)` 验证超时路径，再等待 5 个工作线程全部完成，最终计数归零

**完成标志：** 5 个任务全部完成后 `wait()` 返回，超时路径和归零唤醒都正确

---