## Day 241（Wed）— 多后端负载均衡

**预计时间：1 小时**

**任务：**
- [x] 实现 `LoadBalancer`，持有多个后端的连接池：
  ```cpp
  class LoadBalancer {
      vector<unique_ptr<TcpConnPool>> backends_;
      atomic<int> rr_counter_ = 0;  // Round Robin 计数器
  public:
      shared_ptr<TcpConn> acquire();  // 按策略选一个后端
  };
  ```
- [x] 实现三种策略：
  - **Round Robin**：轮流选取
  - **Least Connections**：选当前活跃连接数最少的后端
  - **Random**：随机选取
- [x] 测试：3 个后端，Round Robin 负载分布是否均匀

> 口径说明：本仓库落点为跨平台模拟（high_perf_net 进程内 `RoundRobinBalancer`/`LeastConnBalancer`，后端为进程内 `Backend` 对象，非真实 TCP），本日 [x] 记录的是负载均衡策略的概念与设计掌握；面向真实 TCP 后端的负载均衡实现属于下一步 Linux 补课，见本模块 README「实现口径」小节（真实 epoll+timerfd 落地参考 raft_kv/net/）。

**完成标志：** 三种策略均可切换，负载分布符合预期

---
## 实践落点

- 代码目录：`CPP-Practice/high_perf_net/`
- 核心文件：`CPP-Practice/high_perf_net/mini_high_perf_net.h`
- 验证入口：`CPP-Practice/high_perf_net/main.cpp`
- 覆盖主题：ConnectionPool、RoundRobin、LeastConn、健康检查

## 验证命令

```powershell
cmake -S CPP-Practice/high_perf_net -B CPP-Practice/high_perf_net/build
cmake --build CPP-Practice/high_perf_net/build --config Release
.\CPP-Practice\high_perf_net\build\Release\high_perf_net_demo.exe
```

