## Day 235（Thu）— SQPOLL 内核轮询模式

**预计时间：1 小时**

**任务：**
- [x] 了解 `IORING_SETUP_SQPOLL`：内核线程轮询 SQ，提交 IO 无需系统调用：
  ```cpp
  struct io_uring_params params = {};
  params.flags |= IORING_SETUP_SQPOLL;
  params.sq_thread_idle = 2000;  // 空闲 2 秒后睡眠
  io_uring_queue_init_params(256, &ring, &params);
  ```
- [x] 分析适用场景：高 IOPS 场景下系统调用开销 vs SQPOLL CPU 消耗
- [x] 测试：SQPOLL 模式下的文件读写吞吐量

> 口径说明：本仓库落点为跨平台模拟（IoUringSimulator 用户态 SQ-CQ 环，无 liburing、无真实 io_uring 系统调用），本日 [x] 记录的是 SQPOLL trade-off 的概念与设计掌握；真实 `IORING_SETUP_SQPOLL` 内核轮询模式与吞吐量实测属于下一步 Linux 补课，见本模块 README「实现口径」小节（真实 epoll+timerfd 落地参考 raft_kv/net/）。

**完成标志：** 理解 SQPOLL 的 trade-off，数据已记录

---
## 实践落点

- 代码目录：`CPP-Practice/high_perf_net/`
- 核心文件：`CPP-Practice/high_perf_net/mini_high_perf_net.h`
- 验证入口：`CPP-Practice/high_perf_net/main.cpp`
- 覆盖主题：io_uring SQ/CQ 概念模拟、提交/完成队列

## 验证命令

```powershell
cmake -S CPP-Practice/high_perf_net -B CPP-Practice/high_perf_net/build
cmake --build CPP-Practice/high_perf_net/build --config Release
.\CPP-Practice\high_perf_net\build\Release\high_perf_net_demo.exe
```

