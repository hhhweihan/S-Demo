## Day 232（Mon）— io_uring 基础原理

**预计时间：1 小时**

**任务：**
- [x] 理解 io_uring 三个核心：
  ```
  SQ（Submission Queue）：用户态提交 IO 请求
  CQ（Completion Queue）：内核填写完成事件
  SQE/CQE：请求/完成条目（ring buffer，避免系统调用）
  ```
- [x] 安装 liburing（`sudo apt install liburing-dev`）
- [x] 实现 Hello io_uring：
  ```c
  struct io_uring ring;
  io_uring_queue_init(256, &ring, 0);
  // 提交一个 read 请求
  struct io_uring_sqe *sqe = io_uring_get_sqe(&ring);
  io_uring_prep_read(sqe, fd, buf, sizeof(buf), 0);
  io_uring_submit(&ring);
  // 等待完成
  struct io_uring_cqe *cqe;
  io_uring_wait_cqe(&ring, &cqe);
  ```
- [x] 测试：用 io_uring 异步读文件

**完成标志：** 成功用 io_uring 读写本地文件

---
## 实践落点

- 代码目录：`CPP-Practice/high_perf_net/`
- 核心文件：`CPP-Practice/high_perf_net/mini_high_perf_net.h`
- 验证入口：`CPP-Practice/high_perf_net/main.cpp`
- 覆盖主题：io_uring SQ/CQ 概念模拟、提交/完成队列

## 验证命令

```powershell
cmake -S CPP-Practice/high_perf_net -B CPP-Practice/high_perf_net/build
cmake --build CPP-Practice/high_perf_net/build --config Release
.\CPP-Practice\high_perf_net\build\Release\high_perf_net_demo.exe
```

