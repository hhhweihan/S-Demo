## Day 1（Mon）— size class 映射设计

**预计时间：1 小时**

**任务：**
- [ ] 创建 `pool_allocator.h`
- [ ] 设计 16 个 size class：8, 16, 24, 32, 48, 64, 80, 96, 128, 192, 256, 384, 512, 768, 1024, >1024
- [ ] 实现 `size_to_class(size_t n)` 函数，返回 0-15 的 index（>1024 返回 -1 表示走大对象）
  ```cpp
  // 示例：n=10 → 对应 16 字节 class（index 1）
  // 用静态查表或简单的 if 链实现，不用过于精妙
  ```
- [ ] 在 `main.cpp` 里打印 1-1200 字节各自对应的 class，验证映射正确

**完成标志：** 映射函数无越界，打印结果符合预期

---