# Day 3（Wed）— chunk 扩展 + 边界检查

## 预计时间：1 小时

**任务：**

- [x] 当 `free_list_ == nullptr` 时，自动申请新 chunk（容量翻倍策略：1, 2, 4, 8...）

  ```cpp
  void FixedAllocator::expand() {
      void* new_chunk = malloc(block_size_ * next_expand_count_);
      // 初始化新 chunk 的 free list
      // 最后一个 block 指向旧 free_list_，完成链表拼接
      // next_expand_count_ *= 2;
      chunks_.push_back(new_chunk);
  }
  ```

- [x] `stats()` 返回：总块数、已分配块数、chunk 数量
- [x] 析构函数：遍历 `chunks_` 逐一 `free`
- [x] 测试：分配超过初始容量，验证自动扩展；验证 stats 数字正确

**本次实现说明：**

- `FixedAllocator` 从单 chunk 改为 `chunks_` 管理多 chunk。
- `deallocate()` 增加跨 chunk 边界检查：指针必须落在某个 chunk 内且按 `block_size_` 对齐。
- `main.cpp` 用 `init(32, 2)` 验证扩容后总块数为 `2 + 4 = 6`，`chunks == 2`。

**完成标志：** 扩展后能继续分配，析构无内存泄漏

---
