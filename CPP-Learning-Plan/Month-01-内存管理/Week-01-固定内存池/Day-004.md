## Day 4（Thu）— 单元测试

**预计时间：1 小时**

**任务（每条测试写一个函数）：**
- [ ] **基础测试**：分配 1000 次，释放 1000 次，stats.used == 0
- [ ] **重复利用测试**：分配 A，释放 A，再分配 B，验证 A == B（地址复用）
- [ ] **扩展测试**：初始容量设为 4，分配 100 次，验证 chunk 数 > 1
- [ ] **混合测试**：随机顺序分配/释放 10000 次，用 set 记录已分配指针，验证无重复分配
- [ ] 用 `assert` 或自己写 `EXPECT_EQ` 宏

**完成标志：** 所有测试通过，无段错误

---