## Day 19（Fri）— 对比 libc++ 实现差异

**预计时间：1 小时**

**任务：**
- [ ] 找到 libc++ 的 `shared_ptr` 实现（GitHub：`llvm-project/libcxx/include/memory`）
- [ ] 对比并回答（写在「本周结果」里）：
  1. libc++ 的控制块有哪两种（`__shared_ptr_pointer` 和 `__shared_ptr_emplace`）？区别是什么？
  2. `make_shared` 为什么比 `shared_ptr<T>(new T)` 少一次内存分配？
  3. libc++ 如何处理 deleter（自定义删除器）？你的实现支持吗？
- [ ] 给你的 SharedPtr 加一个 deleter 模板参数支持（可选）

**完成标志：** 3 个问题有答案

---