## Day 181（Sat）— 深挖 memory_order 用法

**预计时间：2 小时**

**任务：**
- [x] 回到 LevelDB 的 `skiplist.h`，逐行理解所有 `memory_order` 注释
- [x] 尝试构造一个「如果用 relaxed 会出错」的场景（写出来，不需要实际能复现）
- [x] 总结：在「单写多读」场景下，为什么只需要 acquire/release 对，不需要 seq_cst？

**完成标志：** 有书面分析

---
## 实践落点

- 代码目录：`CPP-Practice/leveldb_mini/`
- 核心文件：`CPP-Practice/leveldb_mini/mini_leveldb.h`
- 验证入口：`CPP-Practice/leveldb_mini/main.cpp`
- 覆盖主题：SkipList 随机高度、插入、查找、有序迭代

## 验证命令

```powershell
cmake -S CPP-Practice/leveldb_mini -B CPP-Practice/leveldb_mini/build
cmake --build CPP-Practice/leveldb_mini/build --config Release
.\CPP-Practice\leveldb_mini\build\Release\leveldb_mini_demo.exe
```

