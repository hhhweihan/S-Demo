# xuanyuan_webserver

## 这个目录是干什么的

这里存放早期网络编程学习阶段积累的项目、测试代码和阶段性实现，属于历史归档目录，保留了多个时间段的学习痕迹。

当前可以看到的内容主要包括：

- `2022_testCode`
- `2023_testCode`
- `MiniWebServer`

这个目录主要是早期内容留档，现在更多是回看用，不算当前主线。

## 从哪里开始看

- 如果想看早期阶段性代码，可以先看 `2022_testCode` 和 `2023_testCode`
- 如果想看更聚焦的网络编程项目，可以看 `MiniWebServer`
- 如果只关心当前系统化学习主线，建议先回到仓库根目录阅读 `CPP-Learning-Plan` 和 `CPP-Practice`

有些子目录能直接拿来继续看和跑，有些只是保留下来的阶段性代码，具体还是要进到对应项目里单独判断。
